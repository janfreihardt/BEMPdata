% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_wave.R
\name{get_wave}
\alias{get_wave}
\title{Download and return a BEMP wave dataset}
\usage{
get_wave(wave, format = "csv", refresh = FALSE)
}
\arguments{
\item{wave}{Character. Wave identifier. Use lowercase with an underscore
suffix for migrant (\verb{_M}), non-migrant (\verb{_N}), or village-profile (\verb{_V})
sub-questionnaires. Examples: \code{"w1"}, \code{"w6_M"}, \code{"w12_N"}, \code{"w14_V"}.
See \link{wave_overview} for the full list.}

\item{format}{Character. \code{"csv"} (default) or \code{"dta"} (Stata). CSV returns
a \link[readr:read_delim]{tibble}; DTA returns a labelled tibble via
\link[haven:read_dta]{haven::read_dta} with value labels attached.}

\item{refresh}{Logical. Re-download from Zenodo even if already cached.
Default \code{FALSE}.}
}
\value{
A tibble with one row per survey respondent.
}
\description{
Downloads the requested wave dataset from Zenodo on first use and returns
it as a tibble. All wave files share a single zip archive per format, so
the first call downloads every wave at once (~6 MB for CSV, ~14 MB for
Stata); subsequent calls are instant because files are cached locally.
}
\examples{
\donttest{
# Baseline in-person wave
w1 <- get_wave("w1")

# Wave 6, migrant questionnaire (accepts upper or lower case)
w6m <- get_wave("w6_M")

# Village profile, Wave 14, in Stata format with value labels
w14v <- get_wave("w14_V", format = "dta")
}
}
