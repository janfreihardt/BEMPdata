% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_codebook.R
\name{get_codebook}
\alias{get_codebook}
\title{Get the codebook for one or all BEMP waves}
\usage{
get_codebook(wave = "all", refresh = FALSE)
}
\arguments{
\item{wave}{Character. Wave identifier (e.g. \code{"w1"}, \code{"w6_M"}) or \code{"all"}
(default) to return the merged codebook for every wave. See \link{wave_overview}.}

\item{refresh}{Logical. Re-download from Zenodo even if already cached.
Default \code{FALSE}.}
}
\value{
A tibble with one row per variable. Key columns:
\itemize{
\item \code{wave}: wave identifier
\item \code{variable_name}, \code{variable_label}
\item \code{block}, \code{question}, \code{question_type}
\item \code{question_text}, \code{question_text_bn} (Bangla)
\item \code{appears_in_waves}: cross-wave appearance string
\item Value label columns (\code{val_0}, \code{val_1}, ...)
}
}
\description{
Returns the codebook for a specific wave, or the merged codebook across all
waves. Codebooks are downloaded from Zenodo on first use (~4 MB) and cached
locally. Alternatively, use the pre-built \link{codebook} object that ships with
the package for offline use.
}
\examples{
\dontrun{
# Codebook for the baseline wave
cb_w1 <- get_codebook("w1")

# Merged codebook (all waves)
cb_all <- get_codebook()
}
}
